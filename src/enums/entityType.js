/**
 * @enum entityType
 */
const entityType =
{
    
};
 
export default entityType;
/*
button => clickable element
form => form to be sent, should maybe include form elements?
input => input field, for sending text
label => a label
table => a table, should contain full table creation functionality
iframe => an interactive frame window, to inject html
img => an image
map => a clickable image area
span => a non-blocking element
time => time relevant element
area => a settable area
div => a block element
a => a link
*/